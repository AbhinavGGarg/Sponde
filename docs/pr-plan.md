# PR plan (Qodo-reviewed, in order)

0. init commit on main: README.md, LICENSE, .gitignore, package.json/lockfile, configs, CI — then INSTALL QODO before PR 1.
1. PR `feat/room-server`: src/server/** + test/** — the room, the operator view, the protocol tests.
2. PR `feat/agents-driver`: src/scripts/** — agent manifests, kickoff driver, notifications.
3. PR `docs/demo`: docs/**, README refinements, screenshots.
Fix or answer every Qodo finding in-thread before merging each. AI-assistance disclosure in each PR body.
